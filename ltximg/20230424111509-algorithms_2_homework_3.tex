% Intended LaTeX compiler: pdflatex
\documentclass[a4paper,12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{wrapfig}
\usepackage{rotating}
\usepackage[normalem]{ulem}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{capt-of}
\usepackage{hyperref}
\usepackage{\string~/.emacs.d/common}
\author{mahmood}
\date{\today}
\title{algorithms 2 homework 3}
\hypersetup{
 pdfauthor={mahmood},
 pdftitle={algorithms 2 homework 3},
 pdfkeywords={},
 pdfsubject={},
 pdfcreator={Emacs 28.2 (Org mode 9.5.5)}, 
 pdflang={English}}
\begin{document}

\maketitle
\begin{question}
\begin{answer}
my idea is to simply replace the usual weight function in the relax function of the bellman-ford algorithm call with an external time dependant weight function\\
\includesvg{/home/mahmooz/brain/out/P9jJoKl}\\
\end{answer}
\end{question}
\begin{question}
modify the so that it substitutes \(d[v] = -\infty\) for each vertex for which there exists a negative cycle over some path from the origin vertex to \(v\)\\
show why the algorithm is correct\\
\begin{answer}
im actually not sure of this solution\\
\includesvg{/home/mahmooz/brain/out/lzKwLVk}\\

time complexity of the algorithm isnt affected, for s, the of the algorithm remains \(\Theta\left(|V|^3\right)\) while for sparse graphs its \(\Theta(|V| \cdot |E|)\)\\
\end{answer}
\end{question}
\begin{question}
given an undirected non-negatively weighted graph \(G=(V,E,w)\) that describes the network of \(n\) computers and the direct connections between them. the weight of an edge \((x,y)\) is the transition time of a message from \(x\) to \(y\) in their direct connection. given a vertex \(m \in V\) that represents the network archive. write an efficient algorithm that finds the transition timing matrix of messages in the given network such that each message has to go through the computer \(m\), i.e. it has to find the matrix \(A[n \times n]\) such that \(A[i,j]\) is the distance of the shortest path in the network from \(i\) to \(j\) that goes through \(m\)\\
\begin{answer}
\includesvg{/home/mahmooz/brain/out/owCaG5v}\\
\end{answer}
\end{question}
\begin{question}
propose an algorithm that finds the distances from all vertices to a given vertex \(s\) in an undirected graph whose weights belong to the set \(\{1,2,3,4,5\}\) in \(\Theta(|E|)\) time\\
\begin{answer}
my idea is to turn each \$n\$-weighted edge into \(n\) vertices, so the distance remains the same but the graph is now all edges of weight 1, so we can just use the \(\textsc{BFS}\) algorithm which runs in \(\Theta(|E|)\) time, although by doing this we are increasing the amount of edges in the graph by up to \(5|E|\) but \(\Theta(5|E|)=\Theta(|E|)\) so it doesnt matter\\
we should probably get rid of the new nodes we added or even better clone the graph and manipulate that one instead of modifying the original graph\\
im sure there is a better approach\ldots{} but this is what i could come up with\\
\includesvg{/home/mahmooz/brain/out/OS1RAz0}\\

the time complexity is \(\Theta(|E|)\), the algorithm works because the algorithm \(\textsc{BFS}\) works on unweighted graphs and we basically modified the graph to have weights of only 1 and so it can be referred to as an unweighted graph\\
\end{answer}
\end{question}
\begin{question}
given a directed, weighted graph \(G=(V,E,W)\) and a vertex \(v \in V\), we denote the length of the shortest path that starts at some vertex and ends at \(v\) by \(\varphi(v)=\min\{\delta(u,v) \mid u \in V\}\)\\
note that if the weights of the graph are non-negative then \(\varphi(v)=0\) for every \(v\)\\
write an algorithm that receives a graph with no negative cycles and finds \(\varphi(v)\) for every vertex \(v\) in the graph\\
\includesvg{/home/mahmooz/brain/out/1bEBayI}\\
\end{question}
\end{document}