% Intended LaTeX compiler: pdflatex
\documentclass[a4paper,12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{graphicx}
\usepackage{longtable}
\usepackage{wrapfig}
\usepackage{rotating}
\usepackage[normalem]{ulem}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{capt-of}
\usepackage{hyperref}
\usepackage{\string~/.emacs.d/common}
\author{mahmood}
\date{\today}
\title{training on mnist}
\hypersetup{
 pdfauthor={mahmood},
 pdftitle={training on mnist},
 pdfkeywords={},
 pdfsubject={},
 pdfcreator={Emacs 28.2 (Org mode 9.5.5)}, 
 pdflang={English}}
\begin{document}

\maketitle
\begin{note}
this document and others can be found on my website\\
\url{https://mahmoodsheikh36.github.io/}
\end{note}
got the mnist in csv format from \url{https://www.kaggle.com/datasets/oddrationale/mnist-in-csv}\\
im gonna use a \href{20230502235623-feedforward_neural_network.org}{feedforward neural network}:\\
first we load the dataset using the \texttt{cl-csv} library\\
\lstset{language=Lisp,label= ,caption= ,captionpos=b,numbers=none}
\begin{lstlisting}
(ql:quickload "cl-csv")
(defparameter *train-data* (cl-csv:read-csv (pathname train-data-file)))
(defparameter *test-data* (cl-csv:read-csv (pathname test-data-file)))
(defparameter *train-data* (mapcar (lambda (mylist) (mapcar #'parse-integer mylist)) (cdr *train-data*)))
(defparameter *test-data* (mapcar (lambda (mylist) (mapcar #'parse-integer mylist)) (cdr *test-data*)))
\end{lstlisting}

next we create the network, each image is 28x28 pixels so we need an input layer of that size, and an output layer of size 10 because it needs to output 0-9\\
\lstset{language=Lisp,label= ,caption= ,captionpos=b,numbers=none}
\begin{lstlisting}
(defparameter nw (make-network
                  :input-layer-size (* 28 28)
                  :hidden-layer-size 10
                  :hidden-layers-number 1
                  :learning-rate 0.01
                  :output-layer-size 10))
\end{lstlisting}

with this network we have \(28 \times 28 \times 16 \times 10 = 125440\) weights to update for each training example, its a large network and would take alot of time since we're only using a single CPU thread\\
\lstset{language=Lisp,label= ,caption= ,captionpos=b,numbers=none}
\begin{lstlisting}
(defun test ()
  (network-train
              nw
              (list->vector (mapcar 'cdr *train-data*))
              (list->vector (mapcar (lambda (digit)
                                      (let ((mylist (make-list 10 :initial-element 0)))
                                        (setf (nth digit mylist) 1)
                                        mylist))
                                    (mapcar 'car *train-data*)))))
\end{lstlisting}

i was getting (relatively) good results even after a few epochs
\end{document}